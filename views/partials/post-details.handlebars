<section class="card">
  <a href="/post/{{id}}">
  <header>
    <h3>{{title}}</h3>
    <span>Powered by {{traveller.username}} on {{format_date createdAt}}</span>
  </header>
  </a>
  <p>{{text}}</p>
</section>